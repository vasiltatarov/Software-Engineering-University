namespace Operations
{
    public interface IMathOperations
    {
        int Add(int x, int y);

        double Add(double x, double y, double z);

        decimal Add(decimal x, decimal y, decimal z);
    }
}
